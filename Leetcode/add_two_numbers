# https://leetcode.com/problems/add-two-numbers/description/
# Linked lists - work in process

l1 = [2,4,3]
l2 = [5,6,4]

# Sort reverse

l1.reverse()
l2.reverse()



s = ''.join(str(x) for x in l1)
s2 = ''.join(str(x) for x in l2)
result = int(s)+int(s2)

result = f'{result}'

print (type(result))

b = list(result) #[char for char in result]

print (type(b))
print (b)

